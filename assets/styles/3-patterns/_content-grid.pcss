:root {
    --content-grid--gap: space(20);
}

@utility content-grid {
    display: grid;
    grid-template-columns:
        [full-start] minmax(calc(var(--content-gutter) - var(--content-grid--gap)), 1fr)
        [wide-start] repeat(12, minmax(0, calc((var(--container-lg) - 11 * var(--content-grid--gap)) / 12)))
        [wide-end] minmax(calc(var(--content-gutter) - var(--content-grid--gap)), 1fr)
        [full-end];
    column-gap: var(--content-grid--gap);

    /* Default: all items span the wide (12-col) area */
    > * {
        grid-column: wide;
    }

    > .alignprose {
        max-width: var(--prose--max-width, 65ch);
    }

    > .aligncenter {
        grid-column: wide;
        margin-inline: auto;
        max-width: var(--container-sm);
    }

    > .alignwide {
        grid-column: wide;
        width: 100%;
        margin-inline: auto;
    }

    > .alignfull {
        grid-column: full;
        width: 100%;
        max-width: 100%;
        margin-inline: auto;
    }

    > .alignright {
        grid-column: 8 / 14;
    }

    > .alignleft {
        grid-column: 2 / 8;
    }
}

/* Story singles: full-bleed images respect the page boundary.
 * Galleries, page-header, related-stories and other full-bleed sections
 * stay unaffected — this only constrains image blocks. */
.single-story .content-grid > :is(img, figure.wp-block-image).alignfull {
    grid-column: wide;
    width: 100%;
}
