@utility img-fit {
    position: relative;
    display: block;
    overflow: hidden;

    > * {
        /* Making the image fill the available space */
        position: absolute;
        width: 100%;
        height: 100%;
        inset: 0;
        object-fit: cover;
    }
}
